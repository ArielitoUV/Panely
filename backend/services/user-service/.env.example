
DATABASE_URL=postgresql://notesverb:your_secure_password_here@localhost:5432/notesverb_users
PORT=3002
NODE_ENV=development
# URL para comunicación con el servicio de autenticación
AUTH_SERVICE_URL=http://localhost:3001

JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
CORS_ORIGIN=http://localhost:3000
CORS_CREDENTIALS=true
BCRYPT_ROUNDS=10

# =============================================================================
# EJEMPLOS PARA PRODUCCIÓN (descomenta y modifica para producción)
# =============================================================================

# URL de base de datos de producción
# DATABASE_URL=postgresql://username:password@host:port/database_name

# URL del servicio de autenticación en producción
# AUTH_SERVICE_URL=https://auth.your-domain.com

# Secreto JWT para producción (debe coincidir con el servicio de autenticación)
# JWT_SECRET=your-production-jwt-secret-256-bits-minimum

# CORS en producción (restringe a tu dominio de frontend)
# CORS_ORIGIN=https://your-frontend-domain.com
